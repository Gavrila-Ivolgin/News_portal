1. Создать двух пользователей (с помощью метода User.objects.create_user('username')).

    from users.models import User

    User.objects.create_user(username="Elena", email="elena@yandex.ru")
    <User: Elena>

    User.objects.create_user(username="Gavrila_Ivolgin", email="gavrila@yandex.ru")
    <User: Gavrila_Ivolgin>

2. Создать два объекта модели Author, связанные с пользователями.

    from news.models import Author

    user = User.objects.get(username='Elena')
    Author.objects.create(user=user)


    user = User.objects.get(username="Gavrila_Ivolgin")
    Author.objects.create(user=user)

3. Добавить 4 категории в модель Category.

    Category.objects.create(name="Спорт")

    Category.objects.create(name="Автомобиль")

    Category.objects.create(name="Экономика")

    Category.objects.create(name="Культура")


4. Добавить 2 статьи и 1 новость.

    from news.models import Post

    user_1 = User.objects.get(username="Elena")
    user_2 = User.objects.get(username="Gavrila_Ivolgin")

    author_1, created = Author.objects.get_or_create(user=user_1)
    post_1 = Post.objects.create(author=author_1, type=Post.ARTICLE, title="Какой-то заголовок", text="Какой-то текст", rating=1)

    author_2, created = Author.objects.get_or_create(user=user_2)
    post_2 = Post.objects.create(author=author_2, type=Post.ARTICLE, title="Опять какой-то заголовок", text="Опять какой-то текст", rating=3)

    author_3, created = Author.objects.get_or_create(user=user_2)
    post_3 = Post.objects.create(author=author_2, type=Post.NEWS, title="Очередной заголовок", text="Более интересный текст", rating=2)

5. Присвоить им категории (как минимум в одной статье/новости должно быть не меньше 2 категорий).

    category_1 = Category.objects.get(name="Автомобиль")
    category_2 = Category.objects.get(name="Спорт")
    category_3 = Category.objects.get(name="Культура")
    category_4 = Category.objects.get(name="Экономика")

    # Добавляем категории к записи Post
    post_1.categories.add(category_1, category_4, category_2, category_3)
    post_2.categories.add(category_4, category_2)
    post_3.categories.add(category_3, category_1)

6. Создать как минимум 4 комментария к разным объектам модели Post (в каждом объекте должен быть как минимум один комментарий).

    from news.models import Comment

    comm_1 = Comment.objects.create(post=post_1, user=user_1, text="Это комментарий пользователя Елена к посту <Какой-то заголовок>", rating=1)
    comm_2 = Comment.objects.create(post=post_2, user=user_2, text="Это комментарий пользователя Gavrila_Ivolgin к посту <Опять какой-то заголовок>", rating=2)
    comm_3 = Comment.objects.create(post=post_3, user=user_2, text="Это комментарий пользователя Gavrila_Ivolgin к новости <Более интересный текст>", rating=3)
    comm_4 = Comment.objects.create(post=post_3, user=user_1, text="Это комментарий пользователя Елена к новости <Более интересный текст>", rating=2)

7. Применяя функции like() и dislike() к статьям/новостям и комментариям, скорректировать рейтинги этих объектов.

